Quick instructions how to build mash.
mash is known to compile and work on gcc > 4.6 and clang > 3.0.

Build it in a seperate directory:
  mkdir build
  cd build
  cmake ../
  make

Run it with specific settings:
  vi params.txt
  ./mash

Run and compile the various tests:
  make check

Tarball mash:
  ./bin/smash_tarball
