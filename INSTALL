Quick instructions how to build dash.

Build it in a seperate directory:
  mkdir build
  cd build
  cmake ../
  make

Run it with specific settings:
  cp ../src/params.txt .
  vi params.txt
  ./src/box
